import React, { useState, useEffect, useRef,useContext } from 'react';

import api from '../api';

import SambaSetting from "./samba";

export default function Setting() {
    return <div><SambaSetting/></div>;
}
